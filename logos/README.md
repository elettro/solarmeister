# SolarMeister official logos

Current official logo assets live in this folder.

Each approved lockup is represented in black and white. Use files marked `-print` for high-resolution production and files marked `-web` for digital use. Legacy V1 logo files are stored in `/logos/v1/`.
